# All dimensions are in decimal and relative to frame size (not in pixels).

float32 x           # Center of bounding box on image coordinate x-axis
float32 y           # Center of bounding box on image coordinate y-axis
float32 w           # Width of bounding box
float32 h           # Height of bounding box
float32 conf
uint16 label_id
string label_name